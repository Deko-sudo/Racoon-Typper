# Technical Debt — Racoon Typper

**Last updated:** 2026-08-06
**Scope:** Current debt after the Phase 0–3B.3 baseline commit, Phase 6 CI hardening, the GLib/GTK3 dependency decision (ADR 0001), the SQLite backup/restore data layer (Task B), migration-matrix upgrade evidence (Task C), long-history reporting (Tasks D–E), committed Task F portable profile transfer, the Task G threat-model baseline, the Task H–I security hardening, the Task J hostile-input/error-redaction regression, and the Task K release-workflow split.

## Active debt

| ID | Debt | Owner phase | Priority |
|---|---|---|---|
| TD1 | The private session service is intentionally not a standalone application layer with typed ports/use cases. **Resolved:** `racoon-application` now owns the reporting use cases (`ListTestHistory`, `GetReportingSummary`, `GetAnalyticsSnapshot`, `ListDailyStatistics`, `ListPersonalBests`, `GetStreakReport`, `GetTestDetails`, `GetTestReplayPage`, `BuildTestHistoryExport`) behind the typed `HistoryReportingPort`/`ProgressReportingPort`/`PersonalBestReportingPort`/`AnalyticsReportingPort`. `racoon-data` implements all four SQLite adapters (`crates/data/src/repository/reporting.rs`: `SqliteHistoryReportingPort`, `SqliteProgressReportingPort`, `SqlitePersonalBestReportingPort`, `SqliteAnalyticsReportingPort`, 9 adapter tests). The live dashboard/progress/achievements/insights/consistency commands in `racoon-app` now route through these use cases, preserving the existing local-calendar-day "today" boundary via a `LocalReportingClock` (daily stats/streaks are stored under local dates). History, personal bests, replay, and export commands remain on the direct repositories because the application reporting projections intentionally omit the integer test id and raw mode-config JSON the frontend IPC wire shapes require; wiring those through is deferred with the rest of the IPC contract typification (TD2 / Phase 3 IPC work). | Phase 3 | P1 (resolved) |
| TD2 | IPC uses named endpoint responses, but request/config contracts remain hand-maintained; `mode_config` and scalar setting updates are not a versioned typed algebra. | Phase 3 | P1 |
| TD3 | `App.svelte` still owns broad interaction state; feature stores and explicit cache/session ownership are not established. | Phase 3 | P1 (resolved) |
| TD4 | Durable crash recovery is implemented for the recovery/finalization ledger; backup/restore, restart-safe live completion rewiring, and deterministic injected runtime providers in production are not fully shipped. | Trusted Core | P0/P1 (resolved) |
| TD6 | SQLite backup/restore data-layer API, rotating pre-migration backups (Task B), migration-matrix evidence (Task C), and the Task F portable JSON transfer API/IPC/runbook/Settings UI are implemented. The UI prechecks file metadata, enforces current preview and explicit replace acknowledgement, then reloads after transactional import. Whole-file restore is now exposed through `restore_database`: the command closes the live `Database`, swaps the file via the validated `restore_from_path`, reopens with migrations, and resets the in-memory engine; the Settings UI picks a backup, confirms, and reloads. Platform/manual recovery smoke remains an owner action. Privacy/retention enforcement remains unfinished. Task I adds bounded opt-in redacted backup diagnostics, and Task J makes the `AppError` IPC, display, and debug boundaries use stable public codes/messages rather than dynamic payloads. The Task H application-command ACL limits the local main window to audited frontend commands, but typed/versioned IPC contracts remain Task R work. | Phases 4–5 | P0/P1 (resolved) |
| TD7 | Packaging signing, SBOM/reproducibility evidence, and cross-platform clean-install smoke tests remain release work. | Phase 6 | P0 |
| TD8 | Theme accessibility: `scripts/theme-pack.test.mjs` was updated to cover all 50 themes (was hardcoded to 25, masking failures). All WCAG contrast targets now pass: `--color-text-disabled` (≥3) fixed in 10 themes; `--color-surface-active` corrected from accent colors to raised surfaces in 30 themes (B1 decision — the current character is highlighted with a neutral raised surface, not an accent); `--color-typing-current` darkened in light themes to match; `--color-accent-text`, `--color-typing-incorrect`, `--color-typing-pending`, `--color-focus-ring`, and `--color-text-secondary` contrast fixed across affected themes. The test's `current` check now measures against `--color-surface-active` (the real render surface in TestView.svelte `.char.current`), not `--color-surface-primary`. | Phase 3 | P2 (resolved) |

## Dependency debt

| ID | Debt | Priority | When |
|---|---|---|---|
| ~~DD1~~ | **Resolved (v1.3.2 window):** `rusqlite` 0.31 → 0.39. Zero source changes; regression evidence in migration_matrix 16/16, backup_restore 9/9, migration_and_perf 22/22, session_ledger 36/36. | — | Resolved |
| ~~DD2~~ | **Resolved (v1.3.2 window):** `refinery` 0.8 → 0.9.2. Resolver-coupled with DD1 (core caps rusqlite ≤0.39; 0.40 awaits upstream widening). Migration-matrix evidence as above. | — | Resolved |
| ~~DD3~~ | **Resolved (v1.3.2 window):** `toml` 0.8 → 1.1. Settings roundtrip 26/26, text-pack 11/11, zero source changes. | — | Resolved |
| DD4 | GTK3-related Tauri transitive dependencies have maintenance/advisory debt (RUSTSEC-2024-0429 `glib` unsound + gtk3-rs unmaintained cluster). **Decision recorded:** documented acceptance scoped to the Linux-only Tauri webview backend; the unsound `VariantStrIter` path is transitive and not reached by our code. Revisit on Tauri gtk4-rs/glib≥0.20 migration or any reachable soundness advisory. See `docs/adr/0001-glib-gtk3-advisory.md`. | High → monitored | Phase 5 G5 / upstream Tauri gtk4 migration |

## Resolved foundation debt

| ID | Debt | Resolved in |
|---|---|---|
| TD-OLD1 | `type_text()` unused test helper | Sprint 8.5 |
| TD-OLD2 | `make_text()` unused test helper | Sprint 8.5 |
| TD-OLD3 | Stringly command errors | Sprint 8 |
| TD-OLD4 | Hardcoded mode type | Sprint 6 |
| TD-OLD5 | Nested engine/database completion locking and repeated completion persistence | Phase 2 |
| TD-OLD6 | Stub lesson repository | Phase 2 |
| TD-OLD7 | Missing release profile stripping | Phase 0 |
| TD-OLD8 | Missing daily statistics/streak persistence | Phase 2 |
| TD-OLD9 | Missing consistency/graph output and persisted text length | Phase 2 |
| TD-OLD10 | Command-module raw SQL and monolithic `commands.rs` | Phase 2 |

## Resolved baseline work

| ID | Work | Resolved in |
|---|---|---|
| TD-NEW1 | Durable session identity (UUIDv7), session ledger, completion intents, finalization ledger, startup recovery coordinator, process-crash campaign | Phase 3B.3 |
| TD-NEW2 | GitHub Actions pinned to full commit SHAs; dependabot update group added | Phase 6 partial |
| TD-NEW3 | Toolchain pinning: `rust-toolchain.toml` (Rust 1.96.0), `.nvmrc` + `engines` (Node >=22) | Phase 6 partial |
| TD-NEW4 | CSP removed `unsafe-inline` from `script-src`; inline style exception retained and documented | Phase 5 partial |
| TD-NEW5 | Bug/feature issue templates, PR template, SECURITY.md | Phase 7 partial |
| TD-NEW6 | Data privacy/retention documentation in `docs/data/privacy.md` | Phase 4 partial |
| TD-NEW7 | Unused `@tauri-apps/plugin-shell` dependency removed | Phase 5 |
| TD-NEW8 | GLib/GTK3 advisory chain (DD4, R-009) decided: documented acceptance scoped to the Linux-only Tauri webview backend, with bounded revisit trigger. ADR `docs/adr/0001-glib-gtk3-advisory.md`. | Phase 5 G5 |
| TD-NEW9 | SQLite online backup/restore data-layer API (`crates/data/src/backup.rs`) using rusqlite's Online Backup API for transactionally consistent snapshots; rotating N=5 pre-migration backups with warn-and-continue; restore validates a sibling temporary replacement before atomically replacing the live main file and removing stale WAL/SHM companions. Tauri restore IPC remains unfinished. | Phase 4 G4 |
| TD-NEW10 | Migration matrix evidence (`crates/data/tests/migration_matrix.rs`, 16 tests): every historical schema V1..V7 upgrades cleanly to V9 through the production Refinery runner with era-appropriate seed data; asserts refinery history, full schema (10 tables + 19 indexes across V1..V9), `foreign_keys=ON`, `journal_mode=wal`, epoch data survival incl. the V005 `session_id` backfill, idempotent reopen, and a V1→V9 pre-migration backup round-trip via the Task B seam. FK coverage: `test_replays` `ON DELETE CASCADE`; `session_completion_intents` orphan-insert rejected and `session_ledger` delete blocked when an intent exists (V007 `ON DELETE RESTRICT`); `session_finalizations` orphan-session and mismatched-fingerprint rejected (V008 `ON DELETE RESTRICT` + composite fingerprint FK); `personal_bests.best_wpm_test_id` `NO ACTION` blocks deleting a referenced `tests` row. Observation recorded: `session_ledger` (V006) has no foreign keys by design; FK enforcement begins at V007 (child → ledger) and V008 (composite fingerprint → intents); `personal_bests` references use implicit NO ACTION. | Phase 4 G4 |
| TD-NEW11 | Long-history reporting semantics (Tasks D–E): V009 deterministic ordering indexes; explicit tie-break ordering for history and personal-best lists; 10k planner/timing and >100k complete-aggregate evidence; dashboard/achievement global best and streak reads use maintained projections; intentionally recent consistency window documented in ADR 0002. | Phase 4 G4 |
| TD-NEW12 | Task F release scope: strict versioned JSON (`racoon-typper-profile` schema v1), 64 MiB/100k collection bounds, complete validation before writes, no-write import preview, merge/replace policies, transactional portable-table import, Tauri IPC, recovery/profile-transfer runbook, and a Settings UI with pre-read size checks and explicit replace acknowledgement. It deliberately excludes settings, replays, raw SQLite backups, and recovery/finalization ledgers; whole-file restore remains data-layer only and is tracked in TD6. | Phase 4 G4 |
| TD-NEW13 | Task G threat model: evidence-based inventory of IPC, local files, custom content, diagnostics, packaging, and restore boundaries; records controls, tests, and open residual risks. It cross-references ADR 0001 and does not assert completion of Task H–P controls. See `docs/security/THREAT_MODEL.md`. | Phase 5 G5 (documentation baseline) |
| TD-NEW14 | Task H least-privilege Tauri capability: a build-time `AppManifest` generates per-command permissions for the exact 31 frontend IPC wrappers; the local `main` window receives only those permissions, with no Tauri core window/webview/event/app or remote-origin permission. `crates/app/tests/capability_audit.rs` fails on unexpected or missing handler/frontend/manifest/capability coverage. | Phase 5 G5 |
| TD-NEW15 | Task I redacted local diagnostics: `verbose_logging` is false by default and persists through `SettingsStore`; the backup-failure event uses an allowlisted JSONL schema, bounded rotating files, and no fallback diagnostic write on filesystem errors. Unit tests prove disabled mode, retention bounds, and absence of typed text/profile content/supplied paths. | Phase 5 G5 |
| TD-NEW16 | Task K release-workflow split: `.github/workflows/ci.yml` is PR-checks only (rust/frontend/license, `contents: read`); `release-candidate.yml` rebuilds Linux+Windows artifacts from an immutable tag, validates the tag against the project version, generates `SHA256SUMS`, and creates a draft prerelease; `promote-release.yml` is a `workflow_dispatch` gated by the protected `release-promotion` environment that flips the draft to a published release with generated notes (no rebuild, no secrets). Model and scope limits documented in `docs/release-workflow.md`. SBOM/provenance attach (Task L), signing (TD7), and clean-install smoke (Tasks M/Q) remain separate release tasks. | Phase 6 G6 |
| TD-NEW17 | Task S frontend decomposition: `App.svelte` split into three feature stores — `testSession.svelte.ts` (session lifecycle, key queue, test config, typing warnings, start/abort/restart/custom/lesson/training orchestration with injected side effects), `settings.svelte.ts` (settings/themes loading, theme application incl. the frontend-synthesized custom theme, inline CSS variable bookkeeping), and `content.svelte.ts` (history, bests, custom texts, lessons, weak keys, dashboard loaders). App.svelte shrank 1206 → 546 lines and keeps only navigation, global presentation state, the keydown handler, the test clock, and view wiring. No user-visible behavior change; svelte-check 0/0, build ok, UI tests 29/29. | Phase 3 (TD3) |
| TD-NEW18 | Phase 8.1 weekly summaries slice: `ListWeeklySummaries` aggregates persisted sparse daily statistics into bounded Monday-start ISO week summaries (weighted wpm/accuracy by daily test counts, weekly best wpm, days practiced, goal-met days) inside `racoon-application`, reusing `ProgressReportingPort::load_daily_statistics` with no schema or data-layer changes; empty weeks are reported as zeroed entries so the dashboard strip stays contiguous. Exposed through the audited `get_weekly_summaries` IPC command (1–52 window bound, default 8) and a Dashboard weekly strip with per-week bars and goal-day counters. | Phase 8.1 |
| TD-NEW19 | Phase 8.2 onboarding slice: a one-time first-run gate (shown to every profile until `onboarding_completed` is set, including once after upgrade) collects interface language, practice language, and a daily goal in three steps with an explicit skip; completion persists through the existing audited `set_setting` surface. The new `practice_language` setting (serde-defaulted, no migration) is format-validated by the data layer and membership-checked against bundled course resources in the application command layer; test/lesson language selections initialize from it instead of the hard-coded "en". No new IPC commands; flow logic (step bounds, goal clamping) is covered by `scripts/onboarding-ui.test.mjs`. | Phase 8.2 |
| TD-NEW20 | Phase 8.5 close-out: a resource-completeness regression audit (`crates/resources/tests/resource_completeness.rs`) pins every language in the new `SUPPORTED_COURSE_LANGUAGES` registry to a full vertical slice — course (≥3 modules, ≥12 lessons, unique non-empty lesson ids), quote pack (≥10, attributed), word pack (≥300 entries, <10% duplicates) — failing CI on stub content or drift between the three resource loaders. The audit found and fixed real gaps: 297→307 Czech words and deduplicated generated CJK word lists (zh-tw 2140→1671, zh-hk ~1607→1470). All 15 UI languages ship complete courses/quotes/words; no further language work is outstanding. | Phase 8.5 |
| TD-NEW21 | Phase 8.6 keyboard-layout slice: the `keyboard_layout` setting (`qwerty` \| `jcuken` \| `dvorak`, default `qwerty`; strict enum validation) selects the Latin layout used by finger hints and visualization; Cyrillic characters always resolve to JCUKEN, matching `finger_for_char_with_layout` in racoon-core. Dvorak row/finger tables were added to `frontend/src/lib/keyboard.ts` alongside dispatch helpers, wired into the keyboard trainer, hand guide, result heatmap, weak-keys finger tags, and a Settings selector; table parity and dispatch rules are pinned by `scripts/keyboard-layouts.test.mjs`, and the audit also filled missing ЙЦУКЕН finger entries (й х ж и т э точка). OS-level layout detection and remapping practice texts remain out of scope. | Phase 8.6 |
| TD-NEW22 | Phase 8.7 text-pack interchange slice: versioned `racoon-typper-text-pack` v1 export/preview/import for user-authored custom texts, with sandboxed parsing shared by all three commands (parse → validate → preview without writes → transactional apply), explicit mapping rules for Anki-style TSV, RFC4180 CSV, blank-line text blocks, and the JSON schema (foreign formats/future versions rejected), merge policies scoped per language with a replace acknowledgement, and bounds mirroring the editor (4 MiB / 500 entries / editor name+text limits). New audited IPC surface `export_text_pack` / `preview_text_pack_import` / `import_text_pack`; contract pinned by `scripts/text-pack-contract.test.mjs`; documented in `docs/data/text-packs.md`. Course-sequence composition (multi-module user courses) remains future work. | Phase 8.7 |
| TD-NEW23 | Desktop UI automation harness for release smokes: typed-input persistence cannot yet be asserted on `windows-latest` CI. Groundwork landed (pinned tauri-driver provisioning, selenium-webdriver client driving `.text-display`, msedgedriver matched to the exact WebView2 runtime version), but session creation fails with "DevToolsActivePort file doesn't exist" even under the documented pure upstream flow (`TAURI_WEBVIEW_AUTOMATION` defaults, no manual CDP args) — an EdgeDriver↔WebView2 attach limitation of GitHub runner desktop sessions. Next levers: the WebdriverIO `@wdio/tauri-service` embedded-driver route or CrabNebula's cross-platform tauri-driver fork; until then the Windows smoke keeps its proven install/first-screen/live-session/restart guarantees. | Open — revisit with WebdriverIO service route |

# Racoon Typper — Product Roadmap (от простого к сложному)

Продуктовая дорожная карта. Идём от простых, низкорисковых улучшений к
сложным, требующим инфраструктуры. Каждый этап самодостаточен и проверяем.

**Версия:** 1.2.0 (опубликована)
**Ветка:** `master` (рабочая копия `/home/yago/Desktop/racoon-typper`)
**Обновлено:** 2026-08-21

---

## Принципы

- **От простого к сложному:** сначала то, что можно сделать и проверить за
  один заход, потом то, что требует инфраструктуры/интеграций.
- **Каждый этап = отдельный коммит/PR** с проверкой (`svelte-check`, тесты, CI).
- **Не ломать работающее:** изменения аддитивные, обратная совместимость.
- **Проверяемо:** у каждого этапа есть критерий готовности.

---

## Этап 1 — Гигиена репозитория (простое, срочное) — ✅ DONE

Незакоммиченные UI-правки (календарь, Vim-подсказка, кнопка «Заново»,
guard от повторного запуска) лежат в рабочей директории. Их нужно зафиксировать.

- [x] Закоммитить текущие изменения фронтенда одним PR.
- [x] Добавить в `.gitignore` посторонние файлы агентов: `.hermes/`, `.zcode/`, `rdmglm.md`.
- [x] Проверить, что `git status` чист после коммита.
- [x] Прогнать полный набор проверок (svelte-check, build, тесты, clippy).

**Критерий готовности:** `git status` чист, CI зелёный, изменения в `master`.

**Evidence (2026-08-21):** worktree чист; `.gitignore` содержит `.hermes/`,
`.zcode/`, `rdmglm.md` (`git check-ignore` подтверждает); CI на `master`
зелёный (rust-checks, frontend-build, license-policy); v1.2.0 опубликован.

---

## Этап 2 — Тесты для новых UI-компонентов (простое) — ✅ DONE

Новые компоненты (календарь вкладов, подсказка Vim, кнопка «Заново») без тестов.

- [x] Добавить unit-тесты для логики календаря (уровни интенсивности, построение грида).
- [x] Добавить тесты для `restartTest` (прерывание + перезапуск).
- [x] Проверить, что тесты гоняются в CI.

**Критерий готовности:** новые тесты зелёные, покрытие ключевой логики.

**Evidence (2026-08-21):** `scripts/contribution-calendar.test.mjs` — 6 тестов
(уровни, метрики, грид, тултипы); `scripts/vim-navigation.test.mjs` — 4 теста,
включая хоткей `r` → restart (сам `restartTest` — двухстрочный клей
abandon+start, покрыт через vim-контракт); все гоняются в CI через
`npm run test:ui`.

---

## Этап 3 — Улучшение календаря вкладов (простое)

Сейчас календарь показывает только число тестов в день.

- [ ] Переключатель метрики: тесты / время / уроки.
- [ ] Легенда с числами (как у GitHub: «меньше → больше» с диапазонами).
- [ ] Подсветка сегодняшнего дня.
- [ ] Локализация подписей месяцев (сейчас всегда английские).

**Критерий готовности:** календарь переключает метрики, подписи на языке UI.

---

## Этап 4 — Расширение Vim-режима (среднее)

Сейчас Vim — только навигация h/l/k/j по вкладкам.

- [ ] Режимы (normal/insert) с визуальным индикатором.
- [ ] `gg` / `G` — в начало/конец страницы.
- [ ] `/` — поиск по тексту теста.
- [ ] `r` — перезапуск теста (уже есть кнопка, добавить хоткей).
- [ ] Подсказка в настройках расширяется под новые клавиши.

**Критерий готовности:** Vim-режим покрывает навигацию + базовые команды, подсказка актуальна.

---

## Этап 5 — Автообновление (среднее, высокий UX-эффект) — ✅ DONE

Сейчас пользователь скачивает релиз вручную с GitHub Releases.

- [x] Подключить `tauri-plugin-updater`.
- [x] Настроить endpoint обновлений (GitHub Releases как источник).
- [x] UI: уведомление о доступном обновлении + кнопка «Обновить».
- [x] Проверить на Linux и Windows.

**Критерий готовности:** приложение находит и устанавливает новую версию.

**Evidence (2026-08-21):** `tauri-plugin-updater` подключён (pubkey +
endpoint в `tauri.conf.json`); release-candidate подписывает AppImage/NSIS
и публикует `latest.json` с URL и сигнатурами; Settings UI имеет
«Check for updates» / «Install vX.Y.Z» (`SettingsView.svelte` +
`lib/updater.ts`); артефакты v1.2.0 подписаны (`.sig`).

---

## Этап 6 — Распространение (среднее/сложное)

Релиз только на GitHub Releases. Расширить охват.

- [ ] **Flathub** — Flatpak-манифест уже валидируется в пайплайне; довести до публикации.
- [ ] **AUR** — пакет для Arch (у владельца есть paru/yay).
- [ ] **winget** — публикация для Windows.

**Критерий готовности:** приложение доступно минимум в одном дополнительном канале.

**Статус (2026-08-21):** PKGBUILD — sha256sums закреплён за архивом v1.2.0,
`makepkg --verifysource` и полный локальный `makepkg -f` пройдены
(`docs/PKGBUILD_SOURCE_BLOCKER.md` — resolved). Flatpak-манифест переведён
на GNOME 50 с rust-stable/node22 SDK-расширениями, policy-gate зелёный
(`docs/flatpak-build.md`). Публикация на Flathub/AUR/winget — действия
владельца (аккаунты/ревью).

---

## Этап 7 — Рефакторинг `App.svelte` (сложное)

`App.svelte` разросся (850+ строк). Разбить на feature-модули.

- [ ] Вынести состояние теста в отдельный store.
- [ ] Вынести навигацию/настройки в свои модули.
- [ ] Без изменения пользовательского поведения.

**Критерий готовности:** `App.svelte` заметно меньше, поведение не изменилось, тесты зелёные.

---

## Этап 8 — Продвинутые функции (сложное, по желанию)

- [x] Многопользовательские рекорды / таблица лидеров — **исключено владельцем**.
- [x] Облачная синхронизация профиля между устройствами — **исключено владельцем**.
- [x] Экспорт статистики в сторонние сервисы — **исключено владельцем**.

**Критерий готовности:** определяется на этапе планирования.

---

## Порядок выполнения

Строго по номерам: **1 → 2 → 3 → 4 → 5 → 6 → 7 → 8**.
Каждый этап завершается коммитом/PR и отчётом владельцу перед началом следующего.

## Открытые вопросы

- ~~Автообновление (этап 5): какой источник — только GitHub Releases или нужен свой сервер?~~ — решено: GitHub Releases (done).
- Распространение (этап 6): какой канал приоритетнее — Flathub, AUR или winget? — PKGBUILD/AUR и Flatpak-манифест готовятся в репозитории; публикация — владелец.
- ~~Продвинутые функции (этап 8): что из этого вообще нужно?~~ — решено: исключены (лидерборд, мультиплеер, облако).
